Campo de seleção nativo, sem raio, com glifo `▾` em mono — sem ícone importado.

```jsx
<Select id="sub" label="Submercado" options={['Sudeste/Centro-Oeste','Sul','Nordeste','Norte']} />
<Select id="ciclo" label="Ciclo de apuração" placeholder="Selecione o ciclo" options={[{value:'m',label:'Mensal'},{value:'a',label:'Anual'}]} />
```

Mesmos estados de `Input`: vazio, preenchido, foco, erro, desabilitado.
