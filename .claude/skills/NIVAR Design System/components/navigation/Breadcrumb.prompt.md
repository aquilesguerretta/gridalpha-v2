Trilha em mono versalete.

```jsx
<Breadcrumb items={[
  { label: 'Painel', href: '/' },
  { label: 'Séries', href: '/series' },
  { label: 'PLD horário' },
]} />
```

O último item é o atual, não é link e recebe `--text-strong`. Separador é `/` em `--rule-strong`.
