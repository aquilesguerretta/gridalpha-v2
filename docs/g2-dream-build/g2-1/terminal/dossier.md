# Terminal G2.1 — working reference dossier

Current working surface: `/br/terminal`, structural checkpoint `69a84a5`. Captures were made in a new isolated native Chrome CDP context on 2026-09-11, at 1440 × 1000 and 390 × 844. Existing user/root pages were not reused. The shared Chrome MCP profile was locked; the existing owned Chrome CDP endpoint was usable. No authentication or external product mutation was performed.

## The reference lock

**Primary:** Dovetail's sustained foreground inspector over a quieter plotting surface. **Secondary:** Linear's persistent geometry while explanation changes; Fey's archive for matte substrate and luminous surface edges. **Behavior:** Kepler separates data intake from map manipulation; Windy gives geographic content the continuous field and places time on its boundary.

The current translation is a graphite instrument with a subdued geographic rail, a dominant central chart stage, an independent reading plane, and a selected-observation glass lens. Amber denotes selection. It does not mean gain, loss, availability, or a live feed. Existing sample calculations, source records, exports and unavailable mode remain the source of truth. The map uses the existing IBGE-derived geometry; the grid is a visual registration field, not invented geographic data.

## Captured references

| Reference / specific surface | Capture | Observable mechanism and why it works | Translation / do not copy | Test |
|---|---|---|---|---|
| [Dovetail](https://dovetail.com), product demonstration at scroll 700 | `dovetail-1440-700.png` | Near-black environment, graphite dashboard, and a brighter evidence inspector form three readable planes. The evidence panel extends past the dashboard edge; small citations expose supporting material without replacing the chart. | The instrument receives a solid lifted field and recessed plot. A compact selected-observation lens floats over actual plot geometry; Caderno remains matte explanation. Keep the inspected point visible. Do not import the blue/orange/green semantic palette. | Compare full desktop Terminal and opened source inspector side by side. Is selected evidence visually above the plot? Is the inspected point visible? |
| [Linear](https://linear.app), live hero product demonstration at scroll 450 | `reference-flow/04-linear-motion-0.png` → `05-linear-motion-1.png` → `06-linear-motion-2.png` | The issue, sidebar and agent panel retain their geometry while “Thinking…” resolves into explanatory text and a linked change record. The response appears where attention already is. | Keep map, curve, time control and reading in place. On note selection, the signal changes before the illustrative explanation arrives. No fabricated live events or autonomous activity. | Compare state captures at 80ms, 240ms, and rest; reduced-motion mode must remove these animations. |
| [Origin](https://www.useorigin.com), hero-to-product transition | `origin-1440-700.png` | A continuous cloud field remains behind translucent foreground cards and an inset dark product chapter. The top navigation adapts its tint to the media behind it. | Terminal's source inspector reveals the underlying instrument through a restrained translucent backdrop. Only evidence/context receives glass. No cloud stock imagery or card-stack theater inside operational charts. | Inspect source drawer in both tones. Reading contrast must survive the visible plot behind it. |
| [Fey current site](https://www.fey.com) | `fey-1440-0.png` | The current site says Fey joined Wealthsimple and sign-ups are closed. The former product flow is not available here. | Access limitation, not a live product audit. | Do not describe archived imagery as current behavior. |
| [Fey archived Refero image](https://styles.refero.design/style/bbc8d444-1624-4d01-baba-64e3325f6481) | `reference-flow/07-fey-refero-archive.png` | Dense financial curves recede into a matte black field while a recap floats across the right edge with a narrow bright boundary and visible transparency. | Adopt field/surface hierarchy and data typography. Keep NIVAR's amber selection role and original content. Do not copy market colors, fabricated quotes, or the archival article text. | Place current Terminal beside the actual archived image; inspect material separation at normal viewing size. |
| [Kepler.gl demo](https://kepler.gl/demo), Add Data → Try sample data | `kepler-1440-0.png` → `reference-flow/01-kepler-samples.png` | The geographic workspace persists behind a clear modal. Selecting sample data changes the modal body into named, counted datasets; it does not present a blank map as real coverage. | Keep demonstration and unavailable modes explicit; preserve the region on source inspection and return. | Source drawer Escape restores the original trigger; unavailable removes market values while geography stays selectable. |
| [Windy](https://www.windy.com/?-15.78,-47.93,4), South America wind field and time boundary | `windy-1440-0.png`, `reference-flow/02-windy-current.png` | Geography occupies nearly all available area; controls are attached to its edges. The time label sits immediately above its track. Continuous motion is visually tied to the wind field. | Put the Terminal probe directly beneath its curve and keep the region selected while the sample time changes. Do not add wind particles, live pulses, or a weather palette to synthetic energy values. | Scrub the same sample index across main probe/evidence/region trace. Windy's date-selection attempt did **not** yield a verified changed-time state; `03-windy-time-selection.png` must not be used as proof that the forecast changed. |

Refero's older owner-supplied style UUIDs have moved. The current API-resolved IDs are Fey `bbc8d444-1624-4d01-baba-64e3325f6481`, Linear `554b801c-3b31-4086-a7e5-ae613cdd618b`, Origin `466f6aa2-a9f6-4dcd-9626-73806a01d00e`, and Dovetail `bfe919de-a9a1-4551-bcf7-4d49facc26bd`. Full semantic records are in `refero-styles.json`; they are supporting reference descriptions, not screenshot evidence.

Refero flow `6646`, **New Issue Creation — Priority Selection**, was opened as a three-state archived flow. The full Priority dropdown image was inspected through the connector (`c7d10de1-73d5-4839-841b-33390868fc01`). It demonstrates changing metadata without dismissing the parent task. No live Linear account was accessed.

## Render loop and one-gap corrections

1. **Current structural render:** native desktop and phone inspected before editing. Main criticism: one continuous dark plane; geography, chart and reading have nearly equal material weight. The initial captures in the root of this folder were overwritten by the first candidate; they are not baseline artifacts. Existing structural-checkpoint renders elsewhere in the dossier retain the historical reference.
2. **First candidate:** map registration field, centroid crosshair, chart well, time probe, selected-event rail and a small evidence plate. Comparing it directly with Dovetail exposed one major gap: the evidence plate was too small to establish a sustained foreground plane. Small 8px labels also reduced legibility.
3. **Iteration 03:** selected Literata / Manrope / Geist Mono loaded, small labels raised, all four regional traces visible. Source drawer creates a real context layer. Actual native controls verified across 1440/390, dark/paper, source search, export and unavailable mode.
4. **Iteration 04 — rejected by fresh image-only critic:** making the complete Caderno brighter did not produce legible glass. The independent answer was **NO**, with material hierarchy the largest gap: an opaque rectangular sidebar and an atmospheric wash over nearly equal dark surfaces. See `fresh-craft-critic.md`. The previous builder explanation overstated the result. `iteration-04-desktop.png` and `iteration-04-paper.png` remain as rejected evidence.
5. **Material correction:** removed the global grain from Terminal through a local stacking context and replaced the radial washes with solid planes. Graphite base is RGB 8/12/15, raised working field 36/44/49, and recessed plot 12/19/23. The Caderno is matte. The selected observation moved into a 208px translucent lens over the actual chart: a 50% to 25% graphite tint, 8px blur, one brighter upper edge and contact shadow. The underlying native curve and crosshair remain visible through it. Its upper/lower dock follows the selected value to leave the inspected point clear; the phone lens stays to the left to expose the peak. Paper uses lighter contact shadows. Native captures: `material-pass/1440-dark.png`, `1440-note-03.png`, `1440-paper.png`, `390-dark.png`, `390-probe.png`, and `1440-portal.png`.
6. **Fresh critic 02 rejected the broad field; priority correction:** the independent image-only answer was again **NO** (`../fresh-image-critic-02.md`). The broad gray container still gave the map, controls and reading equal weight, and paper was washed out. Actual Dovetail and Fey images were opened again. The entire outer workspace fill, outer frame and dominant toolbar treatment were removed. Geography now sits at RGB 11/18/22 against the near-black page; central instrument is 23/33/40 and the wider graph is the brightest sustained operational plane at 32/45/53. Reading is an independent, content-height 16/23/28 plane, with two horizontal dividers removed. The existing glass lens remains over the graph. Paper now uses a distinct warm paper graph, darker page, and stronger ink. New native evidence is in `priority-pass/`; the prior `material-pass/` remains a rejected checkpoint. No new critic approval is claimed.
7. **Fresh critic 03 accepted the priority criterion; focused optical correction:** `../fresh-image-critic-03.md` explicitly says the prior surface-priority gap is fixed, while still answering **NO** to exceptional craft. Its one next gap is type at reading scale. This pass preserves the approved planes and serif commentary. Operational buttons and chart axes are 12px; essential source, region, time, unit and supporting labels are 11–12px. Secondary text uses a stronger solid color, at opacity 1, instead of becoming faint over graphite. Physical map labels were measured through the SVG screen transform: 11.68px standalone and 11.28px in the 886px House preview. A local compact-only margin correction prevents the larger phone text from being clipped. Native renders and computed-size records are in `optical-pass/`. No new features, fixture changes or interaction changes were added, and exceptional-craft approval is not claimed.

`comparison.html` and `comparison-depth.png` place actual reference and Terminal renders beside each other. The board uses neutral A/B labels; separate motion rows show captured states. `material-studies.html` uses crops from rendered product states rather than fabricated mockups. The builder does not claim independent critic approval. An independent critic should receive only these renders, the actual motion states, and the harsh-bar questions from the brief.

## Interaction and truth boundaries

- The large number remains the last sample observation. The probe is separately labeled and displays the exact chosen observation. The three notes remain explicitly illustrative hypotheses; free probing does not invent a new causal conclusion.
- Map selection uses unchanged submarket geometry and calls the existing region state. Regional traces are drawn only from `getSeries`; min/max normalization affects the tiny trace geometry, not displayed values.
- Region, metric and period persist through source inspection. Region and event changes animate their visual relationship; no timer advances the sample and no indicator claims a live feed.
- CSV content is unchanged. Native export verified the deterministic seven-day Nordeste load selection and `DEMONSTRACAO_SINTETICA` classification (8 rows including header).
- Unavailable mode removes the plot, selected value, traces and export action while leaving absence explicit. Geometry remains available as geography.
- Phone geography is a deliberate expandable map, not a shrunken desktop dashboard. The time probe is a native range control with a spoken value and keyboard steps.

## Verification record

`iteration-03/verification.json`: 13 named native checks pass; zero browser exceptions. Source search returns the IBGE record, Escape restores trigger focus, map/list state and selected notes agree, probe and evidence agree, download content preserves classification, unavailable remains absent, reduced motion produces no active animations, and Portal preview has no horizontal overflow at 390px. Type gate `npx tsc -b` passes. Scoped `gridalpha-detect` reports no findings.

After the material correction, `material-pass/results.json` records a bounded native pass, not another exhaustive combination run: desktop and 390px renders, note 03, one ArrowRight step, an actual Northeast map-centroid mouse click, the lens's source button, paper mode, and Portal compact render. Native keyboard step changed the value to 222.00 at 20h; map selection then produced Northeast 143.97 at the same time. The source button opened the original dialog. Actual element bounds confirm the selected dot is outside the lens at default/peak and in the compact render. No horizontal overflow and zero browser exceptions. `npx tsc -b` and the scoped detector pass after the structural lens move.

The final phone-only correction shifts the lens right enough to expose the y-axis labels while keeping it left of the peak. `phone-lens.mjs` refreshed the three 390px chart captures after that CSS adjustment; the earlier phone card bounds in `results.json` describe the previous left position. The final native image has visible 248/200/145/90 ticks, visible peak and selected dot. Fresh selected-note motion frames are `material-pass/motion-0.png`, `motion-1.png`, `motion-2.png`; actual capture-start times were 156ms, 343ms and 1046ms after the native click (`motion.json`). These replace the earlier lens-position motion images on `comparison.html`.

`priority-pass/checks.json` records the CSS-only priority correction: native 1440/390 dark and paper renders, actual Northeast centroid click, lens source action, and Portal preview. No horizontal overflow, regional values remain 12px, and zero browser exceptions. The scoped detector reports no findings. No fixture, data transformation, interaction logic or TSX was changed in this correction. The side-by-side and material boards now use `priority-pass/` captures; `material-pass/` motion frames document the unchanged interaction behavior at the previous tonal checkpoint.

`optical-pass/checks.json` is the current visual/type verification: 1440px and 390px standalone, actual 886px desktop House preview and 348px phone House preview, graphite/paper, source drawer, expanded phone geography, and lower phone probe. Visible operational text in the audited set is 11–12px at opacity 1. All four compositions have no horizontal overflow and preserve a visible selected dot. Browser exceptions: zero. `npx tsc -b` and scoped `gridalpha-detect` pass. The side-by-side and material boards now use these optical captures. Hidden SVG labels return unscaled font numbers in the raw audit; those are not measurements of painted phone geography.

The first CSV assertion searched for “AMOSTRA” and lowercase “nordeste”; the actual export intentionally says `DEMONSTRACAO_SINTETICA` and `Nordeste`. The assertion was corrected after inspecting the file; no product data was changed. Early phone captures at 300ms during resize were rejected because they showed an in-progress chart geometry transition. Accepted phone captures wait for the chart to settle.

The later Kepler dataset click displayed the NYC Taxi Trips description while the sample selector remained open (`reference-flow/08-kepler-trip-layer.png`). Despite that filename, it is not evidence of a loaded trip map. The first final recapture was blocked by transient shared-module responses: HeroFilm 404, then FamilyPages 500 during edits. The blank screenshot from that attempt was rejected and removed; the final capture script waits for the real chart to mount before saving a new accepted set.

Screenshot inspection does not establish full accessibility compliance or performance under every GPU/browser. The native keyboard and reduced-motion checks above are specific observed evidence, not a blanket certification.
