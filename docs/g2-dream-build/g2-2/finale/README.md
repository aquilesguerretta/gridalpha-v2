# G2.2 · the open working folio

Local review: [review.html](review.html). Actual runtime: http://127.0.0.1:4173/br — scroll to “A pergunta permanece aberta.”

This package records the implemented closing act, not an approved craft verdict. The independent critic should start with the actual desktop and mobile movies, then the previous ending and Podium's closing, before reading implementation notes.

## Delivered scope

- `src/components/g2/HouseFinale.tsx` and `house-finale.css`: one illustrated closing field, one native evidence folio, five inspectable method traces and a sixth open question. Root integrated the component into the Portal and removed the duplicate Portal footer wordmark/motto.
- New original Diógenes gesture illustration, optimized in two responsive WebP sizes. Full generation prompt and provenance are retained here.
- Actual `/br` recordings and screenshots at 1440×1000 and 390×844, including the preceding chapter, source opening, trace selection and final navigation.

No business logic, authenticated writes, backend data, Alexandria, commits, merge or deployment were performed by this subtask.

## Review order

1. `portal-desktop-motion.mp4` and `portal-mobile-motion.mp4`: actual local Portal, native wheel/pointer input. These are timed browser screenshot frames with original capture intervals, not animated design comps. Reports disclose viewport, sample count and native duration. Capture cadence is lower than the browser's rendering cadence; use the live page to judge frame smoothness.
2. `portal-*-opening.png`, `portal-*-source.png`, `portal-*-resolution.png`: intact stills for legibility and composition.
3. `baseline-desktop.png`, `baseline-mobile.png`: G2.1 baseline, previously observed in the actual Portal.
4. `reference-podium-close.mp4`: independent native capture of the selected live reference. It is research evidence, not licensed runtime media.
5. The live page: open and close “Iluminar origem e limites”; select all five gestures by pointer and keyboard; read the note; inspect the phone layout and reduced motion.

## Questions for the fresh critic

- Does the last chapter lead into a memorable, independent closing act?
- Are face, arm, hand, lantern and evidence a coherent gesture at desktop and phone sizes?
- Do the five families operate on one record, or do the labels carry too much of the explanation?
- Does the source action feel connected to Diógenes's inquiry, and is its information useful?
- Does the ending remain an open investigation rather than a triumphant conclusion?
- Is this a real craft improvement over the original, and does the motion hold up against the selected closing reference?

Give an independent YES/NO with the largest visible weakness. This package does not confer a PASS through implementation completeness.

## Truth boundary

The folio is explicitly didactic: `[68, 64, null, 81, null, 108] MW`, six intervals from 00:00 to 20:00 in a synthetic day. Four measured samples, two absent intervals. The plotted path connects only the first two adjacent samples. No interpolation, fabricated live feed, actual power-plant correspondence or claim about Itaipu is made. The source is visible even while the expanded note is closed.

“Fonte e método” and “Entre na casa” are real existing route links. This is a public educational record; it does not claim to load a user's operational case.

## Verification and limits

- Scoped ESLint passed after final component changes. Root owns combined TypeScript/build checks.
- Six widths (390, 430, 768, 1024, 1440, 1920) passed local geometry checks in the isolated study with actual app CSS: no horizontal overflow, loaded image, exact six samples, four points, one supported segment, two gaps, minimum interactive height 44px desktop / 48px phone. See `responsive-results.json`.
- Native Space key activated each of the five trace buttons with one pressed state and the corresponding readable explanation. See `keyboard-results.json`.
- Actual Portal native pointer capture verified source expanded `true` and Questionar selected, on desktop and mobile. See both motion reports.
- Reduced-motion study verified no figure transform, relative stage position, no source reveal animation, and keyboard opening of the source. Dark-context screenshots use the actual theme switch in the isolated study; the ending remains a light mineral paper field. These are not claimed as full-Portal dark/reduced-motion coverage.
- No full accessibility compliance audit was performed. No complete house-wide motion or hero certification is implied.

The composition is intentionally restrained. Diógenes does not perform a skeletal animation; scroll moves the artwork by at most 28px and changes attention among existing evidence. The source note opens with a short downward mask. Essential material is readable immediately, offscreen RAF work stops, and manual trace selection persists. Whether that restraint achieves the requested exceptional closing is for fresh visual review.

See [storyboard.md](storyboard.md), [rejected-compositions.md](rejected-compositions.md), [asset-provenance.json](asset-provenance.json), and the wider [research observations](../family-finale-research/observations-and-compositions.md).
