# G2 verification record

Completed 2026-09-11 in `C:\dev\gridalpha-v2-nivar-g2`, branch `wave/nivar-g2-dream-build`.

| Check                                                                   | Result     | Evidence / practical limit                                                                                                                                                                                                                                |
| ----------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run build`                                                         | PASS       | Real `tsc -b` gate and Vite production build. `build-final.txt` records existing bundle/CSS/Atlas warnings.                                                                                                                                               |
| Scoped ESLint over every changed G2/account/operator/intake TS/TSX file | PASS       | Zero errors after extracting operator helpers and removing unnecessary seven-row memoization. Not a claim that unrelated repository lint is clean.                                                                                                        |
| `node tools/gridalpha-detect/bin/gridalpha-detect.mjs src`              | PASS       | 690 scanned files; 0 P0, 25 informational P2 in legacy surfaces.                                                                                                                                                                                          |
| `npm run test:stabilization`                                            | 7/7 PASS   | Same-origin API/proxy, shared auth gating and unavailable data behavior.                                                                                                                                                                                  |
| `npm run test:games`                                                    | 14/14 PASS | Existing Alexandria engine and canonical fact checks.                                                                                                                                                                                                     |
| Native public render sweep                                              | 36/36 PASS | Nine public routes × 1440×1000/390×844 × light/dark. Zero exceptions, broken images or horizontal overflow. `public-final/results.json`; current portal details in `portal-final-details/`.                                                               |
| Terminal interactions                                                   | PASS       | 36 region/metric/period combinations; source/search, unavailable state, note selection, completed CSV download; two viewports/themes. `terminal-inspection/interaction-results.json`.                                                                     |
| Account/operator browser fixtures                                       | 24/24 PASS | Actual API client shapes with isolated browser-local responses; identity, products/history, retry, completed file download, logout failure/retry, malformed draft, source replacement/hash binding, export/reload. `account-integration-fixture-qa.json`. |
| Account mobile and real intake fixtures                                 | 29/29 PASS | Four account labels visible; three actual intake openings, retained controls, fonts and two viewports/themes. Zero attempted POSTs/exceptions. `intake-coherence-fixture-qa.json`.                                                                        |
| Real intake business preservation                                       | PASS       | All three pages' state, auth guard, validation and submission logic unchanged; main/form/history unchanged except footer wordmark. `intake-business-preservation.json`.                                                                                   |
| Native film playback                                                    | 10/10 PASS | Six chapters, return after 18s, pause, manual hold, offscreen/hidden pause, reduced-motion start and mobile controls. 14 continuous samples: `motion-runtime/results.json`.                                                                               |
| Dense loop seam                                                         | PASS       | 18 native desktop/mobile samples near actual restart. Outgoing question/table hidden in every sampled measurement frame; zero exceptions. `motion-seam/results.json` and fresh craft R3.                                                                  |
| Alexandria boundary                                                     | PASS       | No Alexandria/global routing/auth/API/backend changes; 18 sampled font/color records match baseline. `screens/alexandria-style-comparison.json`, `screens/alexandria-after.png`.                                                                          |
| `git diff --check`                                                      | PASS       | Whitespace check; ordinary Windows line-ending conversion notices are informational.                                                                                                                                                                      |

Native browser interaction also exercised search (including no results and Escape), mobile navigation/theme, Advisory keyboard tabs, Hardware disclosure, Academy paths, Brief saving/reload and malformed storage/query fallback. Imagery was inspected at full size and in the rendered layout, including the final Academy prop removal.

Fixture harnesses create and dispose isolated browser contexts through Chrome CDP on local port 9235. API interception is limited to the exact application `/api/*` prefix. An early motion harness used a broad path pattern that also intercepted Vite source modules; this harness error was corrected before the passing runs. No real credentials or actual backend writes were used. A separate screenshot wait initially requested lazy image decoding before scrolling; the native capture was repeated with images loaded, and current portal details are in `portal-final-details/`.

The selected film is the native composition at `/br`. The only MP4 is the explicitly rejected six-second object study under `rejected-assets/`; it is not loaded by the product. Prior failed motion evidence remains in `motion-runtime-before-seam-fix/` so R2 findings remain reproducible. Sampled stills do not certify every 60fps frame on every device.

Known limits: Terminal Brasil has no production Brazil feed; the operator queue/local notebook are demonstrative; account editing/recovery retain existing availability; live backend login and customer submission were not used as QA. Global bundle size remains large. No deployment, merge or backend change was performed.
