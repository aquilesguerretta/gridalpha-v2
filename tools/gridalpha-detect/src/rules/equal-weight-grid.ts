import { stubRule } from './_stub.js';

export default stubRule(
  'equal-weight-grid',
  'P2',
  'Equal-weight grid without a dominant focal element.',
  'Equal-weight grids without a dominant focal element',
);
