import { stubRule } from './_stub.js';

export default stubRule(
  'no-box-shadow-on-cards',
  'P1',
  'Drop shadows on data cards. Elevation lives in the four-tier surface tokens.',
  'Drop shadows on data cards',
);
