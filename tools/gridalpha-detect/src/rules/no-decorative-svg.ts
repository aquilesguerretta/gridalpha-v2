import { stubRule } from './_stub.js';

export default stubRule(
  'no-decorative-svg',
  'P0',
  'Decorative SVGs masquerading as data (sine wave hero charts).',
  'Sine-wave decorative SVGs masquerading as data',
);
