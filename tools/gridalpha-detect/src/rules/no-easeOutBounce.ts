import { stubRule } from './_stub.js';

export default stubRule(
  'no-easeOutBounce',
  'P0',
  'Bounce / spring / elastic easing on transitions. Use cubic-bezier(0.4,0,0.2,1).',
  'Bounce / spring / elastic easing',
);
