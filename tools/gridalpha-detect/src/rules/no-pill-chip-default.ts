import { stubRule } from './_stub.js';

export default stubRule(
  'no-pill-chip-default',
  'P1',
  'Pill-shaped chips (border-radius: 9999) for metadata. Use RegimeBadge / StatusDot.',
  'Pill-shaped chips with full border-radius for metadata',
);
