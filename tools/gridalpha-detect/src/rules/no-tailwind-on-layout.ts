import { stubRule } from './_stub.js';

export default stubRule(
  'no-tailwind-on-layout',
  'P0',
  'Tailwind utility classes on layout-critical elements (Generation Mix bar failure).',
  'Tailwind on layout-critical elements',
);
